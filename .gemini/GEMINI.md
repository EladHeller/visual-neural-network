# Project Mandates

- After each code change, always run the project's build, lint, and test commands (`npm run build`, `npm run lint`, `npm test`).
- Ensure no linting errors or test failures are left unresolved.
- For bug fixes, always reproduce the failure with a test case before applying the fix.
